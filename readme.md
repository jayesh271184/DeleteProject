#This is Temporary git repository

```
1. This is first command created to initialize git
	git init

2. for switching from one branch to another branch
       git checkout -b `<branchName>`
       example git checkout -b `dev`
```
